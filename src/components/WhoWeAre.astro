---
const keywords = await Astro.glob('../content/who-we-are/*.md');
---
<ul>
  {keywords.map(kw => <li>
		<h2 class="text-mega">{kw.frontmatter.word}</h2>
		<div set:html={kw.compiledContent()} class="bg-{kw.frontmatter.color}"/>
		<slot/>
		</li>)}
</ul>
